<%= form_for(@info_text) do |f| %>
  <% if @info_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@info_text.errors.count, "error") %> Förhindrade texten från att bli sparad</h2>

      <ul>
      <% @info_text.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.cktext_area :content, :class => 'someclass', :ckeditor => {:language => 'en'} %>

  </div>
  <div class="actions">
    <%= f.submit 'Spara' %>
  </div>
<% end %>
