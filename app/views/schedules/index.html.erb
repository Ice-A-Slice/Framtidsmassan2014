<h1>Alla schema event</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Tid</th>
      <th>Rum</th>
      <th>Utställare</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @schedules.each do |schedule| %>
      <tr>
        <td><%= schedule.time %></td>
        <% @rooms.each do |ex| %>
            <% if ex.id == schedule.rooms_id%>



               <td><%= ex.title %></td>


            <% end %>
        <% end %>
        <% @exhibitors.each do |ex| %>
            <% if ex.id == schedule.exhibitors_id%>


                <td><%= ex.title %></td>


            <% end %>
        <% end %>
        <td><%= link_to 'Visa', schedule %></td>
        <td><%= link_to 'Redigera', edit_schedule_path(schedule) %></td>
        <td><%= link_to 'Ta bort', schedule, method: :delete, data: { confirm: 'Vill du ta bort detta event?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nytt schema event', new_schedule_path %>
